import React from 'react';
import { Grid } from '@material-ui/core';

const Users = () => {
    return (
        <Grid item xs={12}>
            List of all the users
        </Grid>
    );
}

export default Users;
